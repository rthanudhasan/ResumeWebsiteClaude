/** @type {import('next').NextConfig} */
// GEMINI_API_KEY and GEMINI_MODEL are read directly by app/api/route.js on the server.
// Do NOT list them here — next.config's `env` inlines values into the client bundle.
module.exports = {};
